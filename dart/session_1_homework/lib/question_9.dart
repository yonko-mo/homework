// Write code that declares a bool variable called isLoggedIn and assigns it a value of false.
// Then, change the value to true and print the result.

void main() {
  bool isLoggedIn = false;
  isLoggedIn = true;
  print(isLoggedIn);
}
